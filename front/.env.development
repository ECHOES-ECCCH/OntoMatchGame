# Development Environment Configuration
# This file is used when running `npm run dev`

# Base URL for the application (root path for dev)
VITE_BASE_URL=/front/

# API endpoint for development (relative path → routed through the Vite proxy
# defined in vite.config.ts, which forwards /php to http://localhost:8080)
VITE_API_BASE_URL=/php
